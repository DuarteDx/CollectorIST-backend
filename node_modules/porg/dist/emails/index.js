'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _papagaio = require('papagaio');

var _Email = require('./Email');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { return Promise.resolve(value).then(function (value) { step("next", value); }, function (err) { step("throw", err); }); } } return step("next"); }); }; }

const EMAILS_FOLDER = 'emails_templates';

const templateMap = {};

const startConfig = (() => {
  var _ref = _asyncToGenerator(function* ({ emailConfig, templateMap, templatePath }) {
    yield (0, _Email.setConfigEmail)({ emailConfig, templateMap, templatePath });
  });

  return function startConfig(_x) {
    return _ref.apply(this, arguments);
  };
})();

exports.default = (() => {
  var _ref2 = _asyncToGenerator(function* ({ nodeStartPath, emailConfig }) {
    let dirMainFile = _path2.default.dirname(require.main.filename);
    if (nodeStartPath) {
      dirMainFile = _path2.default.resolve(dirMainFile, nodeStartPath);
    }
    let normalizePath = _path2.default.join(dirMainFile, EMAILS_FOLDER);
    if (_fs2.default.existsSync(normalizePath)) {
      _fs2.default.readdirSync(normalizePath).forEach(function (file) {
        const content = _fs2.default.readFileSync(_path2.default.join(normalizePath, file), 'utf8');
        const templateName = file.replace(/.njk$/, '');
        templateMap[templateName] = content;
      });
    } else {
      (0, _papagaio.logger)(['info', 'porg', 'plugins'], 'Porg could not find any emails_templates folder');
    }
    yield startConfig({ emailConfig, templateMap, templatePath: normalizePath });
  });

  return function (_x2) {
    return _ref2.apply(this, arguments);
  };
})();

module.exports = exports['default'];