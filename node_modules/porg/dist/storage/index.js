'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _providers = require('../providers');

var _utils = require('../utils');

let storages = {};

const storageSetup = ({ storageConfig }) => {
  for (const key of Object.keys(storageConfig)) {
    storages[key] = (0, _providers.getStorageProvider)({ name: storageConfig[key].provider, rootPath: storageConfig[key].rootPath });
  }
};

const Storage = {
  // Receives the storage name as it is written on porg.yml
  get: ({ name }) => {
    return storages[(0, _utils.kebabToCamel)({ value: name })];
  }
};

exports.default = {
  storageSetup,
  Storage
};
module.exports = exports['default'];