"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
const getExpandedPathTree = ({ value, folderNameSize }) => {
  const numberOfFolders = Math.ceil(value.length / folderNameSize) - 1;
  let previousIndex = 0;
  let folders = [];
  for (let currentIndex = folderNameSize; currentIndex <= numberOfFolders * folderNameSize; currentIndex += folderNameSize) {
    folders.push(value.slice(previousIndex, currentIndex));
    previousIndex = currentIndex;
  }
  return folders;
};

exports.default = {
  getExpandedPathTree
};
module.exports = exports["default"];