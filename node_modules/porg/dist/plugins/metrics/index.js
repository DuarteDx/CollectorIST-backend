'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.clientProm = exports.plugin = undefined;

var _promClient = require('prom-client');

var _promClient2 = _interopRequireDefault(_promClient);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

let clientProm = _promClient2.default;

const plugin = {
  name: 'metrics-plugin',
  version: '1.0.0',
  register: function (server, options) {
    if (options.enabled) {
      if (options.collectDefaultMetrics) {}
      // Prometheus.collectDefaultMetrics()

      // server.route({
      //   path: '/',
      //   method: 'GET',
      //   handler: (request, h) => {
      //     return h.response(Prometheus.register.metrics())
      //       .header('Content-Type', Prometheus.register.contentType)
      //   },
      //   config: {
      //     description: 'Exposes metrics to Prometheus'
      //   }
      // })
    }
  }
};

exports.plugin = plugin;
exports.clientProm = clientProm;