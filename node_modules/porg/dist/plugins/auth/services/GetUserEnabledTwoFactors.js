'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { return Promise.resolve(value).then(function (value) { step("next", value); }, function (err) { step("throw", err); }); } } return step("next"); }); }; }

exports.default = (() => {
  var _ref = _asyncToGenerator(function* ({ username, getUserU2F, getUserTOTP }) {
    const enabledTwoFactors = [];
    const u2f = yield getUserU2F({ username });
    if (u2f && u2f.keyHandle) {
      enabledTwoFactors.push('u2f');
    }
    const totp = yield getUserTOTP({ username });
    if (totp && totp.secret && totp.confirmed) {
      enabledTwoFactors.push(['totp']);
    }
    return enabledTwoFactors;
  });

  return function (_x) {
    return _ref.apply(this, arguments);
  };
})();

module.exports = exports['default'];