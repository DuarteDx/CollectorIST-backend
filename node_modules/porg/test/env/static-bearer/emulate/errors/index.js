import { setupError } from '@'

export default () => {
  setupError({
    key: 'porg-static-bearer',
    statusCode: 400,
    translations: {
      en: 'Porg static bearer',
      pt: 'Porg static bearer'
    }
  })
}
