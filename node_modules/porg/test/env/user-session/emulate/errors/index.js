import { setupError } from '@'

export default () => {
  setupError({
    key: 'porg-user-session',
    statusCode: 400,
    translations: {
      en: 'Porg user session',
      pt: 'Porg user session'
    }
  })
}
