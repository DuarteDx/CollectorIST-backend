import consul from 'consul'

let consulClient
const configDriver = ({ options }) => {
  try {
    consulClient = consul(options)
  } catch (err) {
    console.log(err)
  }
}

const cleanDatabase = async () => {
  // throw new Error('Clean database not defined')
}

const shutdown = async () => {

}

export {
  configDriver,
  consulClient,
  cleanDatabase,
  shutdown
}
