import Joi from 'joi'

export const mapper = (schema, payload) => {
  const result = Joi.validate(payload, schema, {stripUnknown: true})
  if (result.error) {
    result.error.isSchemaError = true
    throw result.error
  } else {
    return result.value
  }
}
